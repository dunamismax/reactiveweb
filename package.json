{
  "name": "reactiveweb",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "pnpm@10.30.0",
  "engines": {
    "node": ">=24.13.1"
  },
  "scripts": {
    "dev": "node scripts/cli.ts dev web-demo",
    "dev:template": "node scripts/cli.ts dev web-template",
    "dev:app": "node scripts/cli.ts dev",
    "db:migrate": "node scripts/db-migrate.ts",
    "demo:seed": "node scripts/web-demo-seed.ts",
    "demo:bootstrap": "node scripts/web-demo-bootstrap.ts",
    "build": "node scripts/cli.ts build all",
    "typecheck": "node scripts/cli.ts typecheck all",
    "lint": "biome check .",
    "format": "biome check --write .",
    "apps:list": "node scripts/cli.ts list",
    "test:web-demo": "node scripts/cli.ts test web-demo",
    "demo:visual:prepare": "node scripts/web-demo-visual-prepare.ts",
    "test:visual:web-demo:assert:ci": "corepack pnpm --filter @reactiveweb/web-demo run test:visual:ci",
    "test:visual:web-demo": "corepack pnpm run demo:bootstrap && corepack pnpm run demo:visual:prepare && corepack pnpm --filter @reactiveweb/web-demo run test:visual",
    "test:visual:web-demo:ci": "corepack pnpm run demo:bootstrap && corepack pnpm run demo:visual:prepare && corepack pnpm run test:visual:web-demo:assert:ci",
    "test:visual:web-demo:debug": "corepack pnpm run demo:bootstrap && corepack pnpm run demo:visual:prepare && corepack pnpm --filter @reactiveweb/web-demo run test:visual:debug",
    "test:visual:web-demo:update": "corepack pnpm run demo:bootstrap && corepack pnpm run demo:visual:prepare && corepack pnpm --filter @reactiveweb/web-demo run test:visual:update"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.2",
    "@types/node": "^24.5.2",
    "@types/react": "^19.2.7",
    "@types/react-dom": "^19.2.3",
    "typescript": "^5.9.2"
  }
}
